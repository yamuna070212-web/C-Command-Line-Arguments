#include <stdio.h>
#include <string.h>

int main(int argc, char *argv[])
{
    if(argc != 2)
    {
        printf("Usage: program.exe number\n");
        return 1;
    }

    printf("Digits = %lu\n", strlen(argv[1]));

    return 0;
}
Run
program.exe 987654
Output
Digits = 6
